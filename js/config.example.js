// Template for the generated js/config.js — copy this file to js/config.js
// and fill in your own values for local testing without env vars, or just
// run `npm run build`/`npm run dev` (which regenerates js/config.js from
// SUPABASE_URL / SUPABASE_ANON_KEY automatically via scripts/generate-config.js).
//
// js/config.js itself is gitignored and must never be committed.

window.SUPABASE_URL = 'https://your-project-ref.supabase.co';
window.SUPABASE_ANON_KEY = 'your-supabase-anon-key';

// The Bitcoin address the Fund Account screen shows for deposits. Set this to
// an address you control — the built-in default is a placeholder that wallets
// reject, so deposits can't be sent anywhere by accident.
window.BTC_DEPOSIT_ADDRESS = 'your-bitcoin-deposit-address';
